# RobotConfigurationDro

Shows the current robot configuration (waist, elbow, wrist). Will handle all supported configuration types.

```jsx codeDemo
import * as React from "react";
import {RobotConfigurationDro} from "@glowbuzzer/controls";

export default () => <RobotConfigurationDro/>
```
