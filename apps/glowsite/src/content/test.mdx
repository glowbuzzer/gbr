---
slug: test
---

# This is Markdown!

Hello this is an example.


```jsx codeDemo
import * as React from "react";
import {BitFieldDisplay} from "@glowbuzzer/controls";

export default () => <BitFieldDisplay value={111} bitCount={8} labels={['ENABLE', , 'POWER', , , , , 'FAULT']}/>
```

```jsx codeDemo
import * as React from "react";
import {ToolPathTile} from "@glowbuzzer/controls";

export default () => <div style={{width: "500px", height: "500px"}}><ToolPathTile/></div>
```

```jsx codeDemo
import * as React from "react";
import {GCodeTile} from "@glowbuzzer/controls";

export default () => <GCodeTile/>
```
